# Backlog — StackTrack
<!-- Ordered list. Top item = next work. The agent updates status;
     order changes only with user agreement. One item ≈ one task brief. -->

| # | Item | What the user will be able to do after | Status |
|---|------|----------------------------------------|--------|
| 1 | Walking skeleton — Vite + React + TS + PWA shell, lint/test/build wired, empty "Today" screen | Start the app locally on phone or computer and see it load | done (2026-06-11) |
| 2 | Stack management — add/edit/archive items (med/sup, dose, schedule times, group), every add/change/removal recorded with its date | Enter the full real stack; change history accumulates automatically | done (2026-06-11) |
| 3 | Today checklist — today's items grouped by time, tap to mark taken; per-item daily note | Run the daily check-in end-to-end | done (2026-06-11) |
| 4 | Custom metrics — define any number (1–10 rating or free number), log values once per day | Track energy, weight, sleep, etc. daily | done (2026-06-11) |
| 5 | Daily notes — one free-text journal note per day | Record context alongside the day's data | done (2026-06-11) |
| 6 | Metric graphs — any metric over time, with color-coded vertical markers at every stack change (added / changed / removed) | See trends and exactly when the stack changed | done (2026-06-11) |
| 7 | Data export — download all data as JSON (full backup) or CSV (spreadsheets) | Back up data before anything risky; safety net for local-first storage | done (2026-06-12) |
| 8 | Stack view sorting — user-selectable sort options on the Stack screen (e.g. by name, group, time, recently changed) | Order the stack list the way that suits how you scan it | done (2026-06-12) |
| 9 | Date navigation — pick a date (calendar) and view/edit that day's entries (checklist, metrics, notes, journal) | Review or correct any past day, not just today | done (2026-06-12) |
| 10 | Import / restore — load a JSON export back in, replacing current data, with safety snapshot | Move to a new device or recover from data loss using a backup file | done (2026-06-12) |
| 11 | GitHub repo + auto-publish — push code to GitHub; every push runs lint/tests and auto-deploys to the existing Cloudflare Pages URL | Update the live app by committing, with CI guarding every deploy | done (2026-06-12) |
| 12 | Interim device sync — 12a foundations (uid + updatedAt, schema v5) + 12b "Sync from file" merge import | Carry data between own devices today without a backend | awaiting demo |
| 14 | Rewrite repo README to describe StackTrack (currently describes the starter pack) | Repo visitors see what the app actually is | done (2026-06-12) |
| 13 | E2E-encrypted sync backend — 13a server; 13b client crypto; 13c tombstones; 13d sync engine + Sync tab (done 2026-06-12); 13e live two-device demo | Automatic cross-device sync where the server can never read the data | 13a–13d done; awaiting live demo |
| 15 | Multiple groups per item — an item can belong to many purpose groups (tag chips); the "by group" view lists it under each, marked "in N groups"; graph markers batch per group (schema v8) | Organize one item under several purposes (e.g. Bone + Immune) | done (2026-06-16) |
| 16 | Today-card polish + optional dose unit + persistent per-item note (waves 1–2 + W3-A) | Cleaner Today cards (badge + groups, tidy layout); enter a unit (shown "500 mg"); pin a note shown on Today | done (2026-06-16) |
| 17 | Composite measurements + boolean kind + rename Metrics→"Tracking" (wave 3, parts B–D) | Track blood pressure (120/80) and other multi-number measurements, plus yes/no things; one "Tracking" page | done (2026-06-16) — shipped/live |
| 18 | Health events — log discrete dated events (symptom/appointment/procedure/other), many per day, on the Today screen; shown as color-coded markers on the metric graphs (schema v9, new healthEvents table) | Record "Fever", "GI Doc Appointment", "Appendectomy" and see them lined up against metric trends | done (2026-06-16) — shipped/live |
| 19 | Collapsible Today sections — Events, Journal, Daily metrics, and "Also recorded" collapse with state remembered (localStorage) | Tidy the Today screen by hiding sections you're not using | done (2026-06-16) — shipped/live |
| 20 | Push / dose reminders (FUTURE) — daily "take your dose" notifications. Needs a push backend: Cloudflare Cron Trigger + web-push + a subscription endpoint + a custom service worker (injectManifest). iOS delivers only when the PWA is installed to the Home Screen; no reliable client-only scheduling API exists | Get reminded to take meds/supplements on time | planned (researched 2026-06-16) |
| 21 | Apple Health sync (FUTURE) — read/write Health data. NOT possible from a pure PWA (no web API); requires wrapping the app in a native Capacitor shell + a HealthKit plugin + App Store distribution (needs a Mac/Xcode) | Pull weight/sleep/etc. from Apple Health instead of typing them | planned (researched 2026-06-16) |
| 23 | Metric notes — a persistent note on a metric definition (shown when logging) + a per-day metric note (one per metric per day), mirroring how meds have both | Add context to a metric ("measured before coffee") and record per-day context ("after a run") | done (2026-06-18) — schema v10 |
| 24 | Med/supplement frequency — every N days, specific days of week, and on/off cycles (N on / N off); Today and any dated view show an item only on the days it's actually due | Schedule things that aren't strictly daily (every other day, Mon/Wed/Fri, 3 weeks on / 1 off) | done (2026-06-18) |
| 26 | Navigation restructure — bottom bar holds the "view" screens (Today, Graphs); a top-right settings cog (⚙) on every screen header opens a menu to the "set up" screens (Stack, Tracking, Sync; Reminders joins with #25). Declutters the bottom bar | Reach setup screens from a tidy settings menu instead of a crowded 5-tab bar | done (2026-06-18) |
| 25 | In-app reminders — once / every-N-days / cycle recurrence + time-of-day + snooze; managed on a Reminders screen (cog menu), surfaced as a Today advisory with Done/Snooze; rides export/import/merge/sync; data model shaped so OS push (item 20) can deliver them later | Queue up "cycle off KSM-66 this week", "refill prescription", "blood test July 1" and see them when due | done (2026-06-18) — Task A (nav #26), Task B (core), Task C (per-occurrence history) all shipped |
| 22 | Sync: chunk the first push — `runSync` sends all changed-since-watermark records in ONE POST, but the server caps a push at 2000 changes (workers/sync logic.ts MAX_CHANGES_PER_PUSH → HTTP 400). A first sync of a large local backlog (a year ≈ 10k+ records) fails permanently. Fix: split the outgoing push into batches (≤500) and loop, reusing the pull-drain `while (more)` loop in syncEngine.ts (~149–224); extend the fake server in tests/syncEngine.test.ts to enforce the cap + assert multiple push requests. (Day-to-day incremental sync is fine — deltas only; this is first-sync-of-a-backlog only. Long-term D1 growth is unbounded but negligible for years — no pruning needed yet.) | Reliably turn on sync after months of offline use without the first sync failing | planned (researched 2026-06-16) |
